Лабораторная работа 3.1. Организация асинхронного взаимодействия микросервисов с помощью брокера сообщений
Наумова Надежда ЦИБ-241
Вариант 14

# Задачи

## 1. Обработка данных IoT

Producer отправляет JSON с температурой.
gRPC сервис проверяет, находится ли температура в диапазоне 18–25°C, и возвращает:

* `"NORMAL"` — если температура в норме
* `"ALARM"` — если температура выходит за пределы нормы

---

## 2. Определение четности

Producer отправляет число.
gRPC сервис определяет, является ли число четным или нечетным, и возвращает:

* `"EVEN"` — четное число
* `"ODD"` — нечетное число

---

## 3. Слияние двух текстов

Producer отправляет два текста.
gRPC сервис объединяет тексты в одну строку и возвращает результат.

---

# Архитектура системы

```text
Producer → RabbitMQ → Consumer → gRPC Server
```

---

# Общая логика работы

1. Пользователь вводит сообщение в Producer
2. Producer отправляет сообщение в очередь RabbitMQ
3. Consumer получает сообщение из очереди
4. Consumer передает сообщение в gRPC сервис
5. gRPC сервер определяет тип задачи
6. Выполняется обработка данных
7. Результат возвращается Consumer
8. Consumer выводит результат пользователю

---

# Используемые технологии

* Python
* gRPC
* RabbitMQ
* Docker
* Pika

---

# ЧАСТЬ 1. Реализация gRPC (синхронное взаимодействие)

Создан файл `message_service.proto`, в котором описан сервис обработки сообщений.

В `.proto` файле реализован метод:

* `ProcessMessage`

Сгенерированы Python-файлы:

* `message_service_pb2.py`
* `message_service_pb2_grpc.py`

с использованием `grpc_tools`.

---

# Реализована логика обработки сообщений

## 1. Проверка температуры IoT

Сервис принимает JSON с температурой:

```json
{"temperature": 30}
```

Если температура:

* меньше 18°C
* больше 25°C

то возвращается:

```text
ALARM
```

иначе:

```text
NORMAL
```

---

## 2. Проверка четности числа

Сервис принимает число и определяет:

* четное → `EVEN`
* нечетное → `ODD`

---

## 3. Слияние текстов

Сервис принимает два текста через разделитель `|`:

```text
hello|world
```

и объединяет их:

```text
helloworld
```

---

# Реализован gRPC сервер

Сервер:

* принимает запросы
* определяет тип данных
* выполняет обработку
* возвращает результат клиенту

Сервер запускается на порту:

```text
50051
```

---

# ЧАСТЬ 2. Реализация RabbitMQ (асинхронное взаимодействие)

Создан файл `docker-compose.yml` и выполнен запуск RabbitMQ в Docker-контейнере.

Использовались порты:

* `5672` — подключение клиентов
* `15672` — web-интерфейс RabbitMQ

---

# Реализован Producer

Producer:

* принимает данные от пользователя
* отправляет сообщения в очередь RabbitMQ

Примеры сообщений:

## 1. IoT температура

```text
{"temperature": 30}
```

---

## 2. Проверка четности

```text
10
```

---

## 3. Слияние текстов

```text
hello|world
```

---

# Реализован Consumer

Consumer:

* получает сообщения из очереди
* вызывает gRPC сервис
* получает результат обработки
* выводит результат пользователю

---

# В ходе тестирования были запущены

* RabbitMQ
* gRPC сервер
* Consumer
* Producer

---

# Были выполнены запросы по всем задачам

---

# Вывод

В ходе выполнения лабораторной работы была реализована система микросервисного взаимодействия, использующая синхронный и асинхронный подходы.

С помощью gRPC реализовано синхронное взаимодействие между сервисами по модели «запрос–ответ». gRPC сервер выполняет обработку сообщений и возвращает результат клиенту.

С использованием RabbitMQ реализовано асинхронное взаимодействие между компонентами системы. Producer отправляет сообщения в очередь RabbitMQ, а Consumer получает сообщения и передает их на обработку gRPC сервису.

Такая архитектура обеспечивает:

* слабую связанность сервисов
* устойчивость системы
* возможность масштабирования
* независимую обработку сообщений

Все три задания варианта 14 успешно реализованы:

* обработка IoT температуры
* определение четности числа
* слияние двух текстов

Система корректно выполняет передачу и обработку сообщений через RabbitMQ и gRPC.
